{{ theme['css_scripts'] }}

.profile-page .bg-white {
    background: var(--color-white) !important;
}

.unsubscribe-section {
    margin: 0 !important;
}

{# .page-login #grav-login {
.form-field:first-child{
    &:before{
    content: "{{ 'METRICALO.USERNAME_EMAIL'|t }}";
    }
}
.form-field:nth-child(2){
    &:before{
    content: "{{ 'METRICALO.PASSWORD'|t }}";

    }
} #}
